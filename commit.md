Commit the changes. Check if it makes sense to group changes into smaller commits. If so, define the group first, identify a commit message and then perform commits.
Once all commits are completed, review details on git remotes and push upstream as needed.
